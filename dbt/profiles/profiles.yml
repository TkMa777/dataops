epsi_dataops:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      user: airflow
      password: airflow
      port: 5432
      dbname: airflow_db
      schema: public
      threads: 4 

sales_pipeline:
  target: dev
  outputs:
    dev:
      type: postgres
      host: postgres
      port: 5432
      user: airflow
      password: airflow
      dbname: airflow_db
      schema: public
      threads: 4
      keepalives_idle: 0
      connect_timeout: 10 